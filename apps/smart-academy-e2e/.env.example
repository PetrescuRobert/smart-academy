# Example .env for E2E tests
# Replace placeholder values with your local database credentials
# DO NOT commit real credentials. Treat any exposed credentials as compromised and rotate them immediately.
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/smart-academy
