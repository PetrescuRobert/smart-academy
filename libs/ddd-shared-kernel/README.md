# ddd-shared-kernel

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ddd-shared-kernel` to build the library.

## Running unit tests

Run `nx test ddd-shared-kernel` to execute the unit tests via [Jest](https://jestjs.io).
